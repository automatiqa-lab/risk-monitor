"""
Operations Risk Monitor - Agent harness.

Each agent implements a collect → filter → summarize → compose → save pipeline
producing risk reports in agent-specific formats (HTML/MD/PDF, PPTX, etc.).
"""
